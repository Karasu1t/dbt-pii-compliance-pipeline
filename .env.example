# Copy to .env (gitignored) and fill in real values. Never commit .env or
# paste these values into a chat session — set them directly in this file
# or in your shell.

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

DATABRICKS_HOST=
DATABRICKS_HTTP_PATH=
DATABRICKS_TOKEN=
